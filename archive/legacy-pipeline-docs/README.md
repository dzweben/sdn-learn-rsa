# archive_root_legacy

This folder stores legacy LEARN markdown files that previously sat at repository root.

Why this exists:
- keep repository root clean and navigable
- preserve historical notes without mixing them into active canonical docs

Current canonical docs live in:
- `rsa-learn/docs/`

Do not use files in this folder as production source-of-truth.
